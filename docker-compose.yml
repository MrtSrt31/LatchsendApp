version: '3.8'
services:
  latchsend:
    build: .
    ports:
      - "3000:3000"
      - "8080:8080"
    environment:
      - DATABASE_URL=file:./dev.db
      - SIGNAL_PORT=8080
    volumes:
      - ./uploads:/app/uploads
      - ./prisma:/app/prisma
    restart: unless-stopped
